<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>                                   <!-- Einsetzten der vordefinierten Methode title -->
    <%= csrf_meta_tag %>
    <%= render 'layouts/stylesheets' %>
  </head>
  <body>
   <div class="container">
     <%= render 'layouts/header' %>
      <section class="round">
        <%= yield %>             <!-- Einbindung der pages-Inhalte -->
      </section>
     <%= render 'layouts/footer' %>
     <%= debug(params) if Rails.env.development? %>
   </div>
  </body>
</html>

